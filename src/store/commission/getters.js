export const commissions = (state) => {
    return state.commissions
}

export const commission = (state) => {
    return state.commission
}

export const meta = (state) => {
    return state.meta
}

export const pending = (state) => {
    return state.pending
}
