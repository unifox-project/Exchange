export const wallets = (state) => {
    return state.wallets
}

export const wallet = (state) => {
    return state.wallet
}

export const checkPending = (state) => {
    return state.checkPending
}

export const meta = (state) => {
    return state.meta
}

export const pending = (state) => {
    return state.pending
}
